# Local Claude Code config — contains API token, never commit
.claude/

# Backup of the former embedded git repo (history kept locally only)
mobile/.git-backup-embedded/

# Local build output / scratch logs — never commit
*.apk
mobile/build-log.txt
mobile/build-result.json
mobile/build-run.log
mobile/dev-server.log
mobile/eas-install-log.txt
web/preview.log
